services:
  signature-remove-bg:
    build: .
    ports:
      - "${PUBLIC_PORT:-8000}:8000"
    restart: unless-stopped
    mem_limit: 128m
    cpus: 1
    env_file:
      - path: .env
        required: false
